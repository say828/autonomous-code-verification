"""Bug corpus for verification benchmarks.

30 Python functions, each with correct and buggy versions.
Derived from the cross-provider verification experiment corpus.
"""

CORPUS = [
    {
        "id": "001",
        "name": "count_vowels",
        "category": "string",
        "difficulty": "easy",
        "bug_type": "missing_edge_case",
        "description": "Count vowels in a string",
        "bug_description": "Missing .lower() - misses uppercase vowels",
        "correct_code": "def count_vowels(s):\n    vowels = 'aeiou'\n    count = 0\n    for c in s:\n        if c.lower() in vowels:\n            count += 1\n    return count",
        "buggy_code": "def count_vowels(s):\n    vowels = 'aeiou'\n    count = 0\n    for c in s:\n        if c in vowels:\n            count += 1\n    return count",
    },
    {
        "id": "002",
        "name": "factorial",
        "category": "math",
        "difficulty": "easy",
        "bug_type": "off_by_one",
        "description": "Compute factorial of a non-negative integer",
        "bug_description": "Range starts at 2 but misses multiplying by n (range end should be n+1)",
        "correct_code": "def factorial(n):\n    if n <= 1:\n        return 1\n    result = 1\n    for i in range(2, n + 1):\n        result *= i\n    return result",
        "buggy_code": "def factorial(n):\n    if n <= 1:\n        return 1\n    result = 1\n    for i in range(2, n):\n        result *= i\n    return result",
    },
    {
        "id": "003",
        "name": "reverse_sublists",
        "category": "list",
        "difficulty": "hard",
        "bug_type": "off_by_one",
        "description": "Reverse each contiguous sublist of positive numbers between zeros",
        "bug_description": "End index is off by one when slicing the sublist to reverse",
        "correct_code": "def reverse_sublists(lst):\n    result = lst[:]\n    i = 0\n    while i < len(result):\n        if result[i] == 0:\n            i += 1\n            continue\n        j = i\n        while j < len(result) and result[j] != 0:\n            j += 1\n        sub = result[i:j]\n        sub.reverse()\n        result[i:j] = sub\n        i = j\n    return result",
        "buggy_code": "def reverse_sublists(lst):\n    result = lst[:]\n    i = 0\n    while i < len(result):\n        if result[i] == 0:\n            i += 1\n            continue\n        j = i\n        while j < len(result) and result[j] != 0:\n            j += 1\n        sub = result[i:j - 1]\n        sub.reverse()\n        result[i:j - 1] = sub\n        i = j\n    return result",
    },
    {
        "id": "004",
        "name": "merge_dicts",
        "category": "dict",
        "difficulty": "easy",
        "bug_type": "wrong_variable",
        "description": "Merge two dicts; second dict values take priority",
        "bug_description": "Uses d1 instead of d2 in second loop, so d2 values are ignored",
        "correct_code": "def merge_dicts(d1, d2):\n    result = {}\n    for k, v in d1.items():\n        result[k] = v\n    for k, v in d2.items():\n        result[k] = v\n    return result",
        "buggy_code": "def merge_dicts(d1, d2):\n    result = {}\n    for k, v in d1.items():\n        result[k] = v\n    for k, v in d1.items():\n        result[k] = v\n    return result",
    },
    {
        "id": "005",
        "name": "find_max_in_array",
        "category": "array",
        "difficulty": "easy",
        "bug_type": "missing_edge_case",
        "description": "Find maximum value in a numeric array",
        "bug_description": "Initializes max_val to 0 instead of first element; fails for all-negative arrays",
        "correct_code": "def find_max_in_array(arr):\n    if not arr:\n        return None\n    max_val = arr[0]\n    for val in arr[1:]:\n        if val > max_val:\n            max_val = val\n    return max_val",
        "buggy_code": "def find_max_in_array(arr):\n    if not arr:\n        return None\n    max_val = 0\n    for val in arr:\n        if val > max_val:\n            max_val = val\n    return max_val",
    },
    {
        "id": "006",
        "name": "is_valid_email",
        "category": "validation",
        "difficulty": "easy",
        "bug_type": "logic_inversion",
        "description": "Basic email validation: must have exactly one @ and at least one dot after @",
        "bug_description": "Uses 'and' instead of 'or' in the invalid check, so partial invalids pass",
        "correct_code": "def is_valid_email(email):\n    if email.count('@') != 1:\n        return False\n    local, domain = email.split('@')\n    if not local or not domain:\n        return False\n    if '.' not in domain:\n        return False\n    return True",
        "buggy_code": "def is_valid_email(email):\n    if email.count('@') != 1:\n        return False\n    local, domain = email.split('@')\n    if not local and not domain:\n        return False\n    if '.' not in domain:\n        return False\n    return True",
    },
    {
        "id": "007",
        "name": "celsius_to_fahrenheit",
        "category": "conversion",
        "difficulty": "easy",
        "bug_type": "wrong_variable",
        "description": "Convert Celsius temperature to Fahrenheit",
        "bug_description": "Adds 23 instead of 32 (transposed digits)",
        "correct_code": "def celsius_to_fahrenheit(c):\n    ratio = 9 / 5\n    offset = 32\n    result = c * ratio + offset\n    return round(result, 2)",
        "buggy_code": "def celsius_to_fahrenheit(c):\n    ratio = 9 / 5\n    offset = 23\n    result = c * ratio + offset\n    return round(result, 2)",
    },
    {
        "id": "008",
        "name": "compute_mean",
        "category": "statistics",
        "difficulty": "easy",
        "bug_type": "missing_edge_case",
        "description": "Compute arithmetic mean of a list of numbers",
        "bug_description": "No check for empty list, causes ZeroDivisionError",
        "correct_code": "def compute_mean(numbers):\n    if not numbers:\n        return 0.0\n    total = 0.0\n    for num in numbers:\n        total += num\n    return total / len(numbers)",
        "buggy_code": "def compute_mean(numbers):\n    total = 0.0\n    for num in numbers:\n        total += num\n    return total / len(numbers)",
    },
    {
        "id": "009",
        "name": "title_case",
        "category": "text",
        "difficulty": "easy",
        "bug_type": "logic_inversion",
        "description": "Convert string to title case (first letter of each word uppercase)",
        "bug_description": "Uses .lower() instead of .upper() on first character",
        "correct_code": "def title_case(s):\n    words = s.split()\n    result = []\n    for word in words:\n        if word:\n            result.append(word[0].upper() + word[1:].lower())\n    return ' '.join(result)",
        "buggy_code": "def title_case(s):\n    words = s.split()\n    result = []\n    for word in words:\n        if word:\n            result.append(word[0].lower() + word[1:].lower())\n    return ' '.join(result)",
    },
    {
        "id": "010",
        "name": "binary_search_range",
        "category": "algorithm",
        "difficulty": "hard",
        "bug_type": "off_by_one",
        "description": "Find first and last position of target in sorted array using binary search",
        "bug_description": "Right boundary search uses mid-1 instead of mid, missing the rightmost occurrence",
        "correct_code": "def binary_search_range(arr, target):\n    def find_left(arr, target):\n        lo, hi = 0, len(arr) - 1\n        result = -1\n        while lo <= hi:\n            mid = (lo + hi) // 2\n            if arr[mid] == target:\n                result = mid\n                hi = mid - 1\n            elif arr[mid] < target:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n        return result\n    def find_right(arr, target):\n        lo, hi = 0, len(arr) - 1\n        result = -1\n        while lo <= hi:\n            mid = (lo + hi) // 2\n            if arr[mid] == target:\n                result = mid\n                lo = mid + 1\n            elif arr[mid] < target:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n        return result\n    left = find_left(arr, target)\n    right = find_right(arr, target)\n    return (left, right)",
        "buggy_code": "def binary_search_range(arr, target):\n    def find_left(arr, target):\n        lo, hi = 0, len(arr) - 1\n        result = -1\n        while lo <= hi:\n            mid = (lo + hi) // 2\n            if arr[mid] == target:\n                result = mid\n                hi = mid - 1\n            elif arr[mid] < target:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n        return result\n    def find_right(arr, target):\n        lo, hi = 0, len(arr) - 1\n        result = -1\n        while lo <= hi:\n            mid = (lo + hi) // 2\n            if arr[mid] == target:\n                result = mid - 1\n                lo = mid + 1\n            elif arr[mid] < target:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n        return result\n    left = find_left(arr, target)\n    right = find_right(arr, target)\n    return (left, right)",
    },
    {
        "id": "011",
        "name": "is_palindrome",
        "category": "string",
        "difficulty": "easy",
        "bug_type": "logic_inversion",
        "description": "Check if a string is a palindrome (case-insensitive, ignoring spaces)",
        "bug_description": "Uses != instead of == in the return comparison",
        "correct_code": "def is_palindrome(s):\n    cleaned = s.lower().replace(' ', '')\n    left = 0\n    right = len(cleaned) - 1\n    while left < right:\n        if cleaned[left] != cleaned[right]:\n            return False\n        left += 1\n        right -= 1\n    return True",
        "buggy_code": "def is_palindrome(s):\n    cleaned = s.lower().replace(' ', '')\n    left = 0\n    right = len(cleaned) - 1\n    while left < right:\n        if cleaned[left] == cleaned[right]:\n            return False\n        left += 1\n        right -= 1\n    return True",
    },
    {
        "id": "012",
        "name": "gcd",
        "category": "math",
        "difficulty": "easy",
        "bug_type": "wrong_variable",
        "description": "Compute greatest common divisor using Euclidean algorithm",
        "bug_description": "Returns b instead of a when b becomes 0",
        "correct_code": "def gcd(a, b):\n    a = abs(a)\n    b = abs(b)\n    while b != 0:\n        temp = b\n        b = a % b\n        a = temp\n    return a",
        "buggy_code": "def gcd(a, b):\n    a = abs(a)\n    b = abs(b)\n    while b != 0:\n        temp = b\n        b = a % b\n        a = temp\n    return b",
    },
    {
        "id": "013",
        "name": "flatten_list",
        "category": "list",
        "difficulty": "medium",
        "bug_type": "wrong_variable",
        "description": "Flatten a nested list one level deep",
        "bug_description": "Appends the sublist itself instead of extending with its elements",
        "correct_code": "def flatten_list(nested):\n    result = []\n    for item in nested:\n        if isinstance(item, list):\n            result.extend(item)\n        else:\n            result.append(item)\n    return result",
        "buggy_code": "def flatten_list(nested):\n    result = []\n    for item in nested:\n        if isinstance(item, list):\n            result.append(item)\n        else:\n            result.append(item)\n    return result",
    },
    {
        "id": "014",
        "name": "count_word_frequency",
        "category": "dict",
        "difficulty": "easy",
        "bug_type": "wrong_variable",
        "description": "Count frequency of each word in a string",
        "bug_description": "Uses text.split() without .lower(), so 'The' and 'the' are counted separately",
        "correct_code": "def count_word_frequency(text):\n    freq = {}\n    for word in text.lower().split():\n        freq[word] = freq.get(word, 0) + 1\n    return freq",
        "buggy_code": "def count_word_frequency(text):\n    freq = {}\n    for word in text.split():\n        freq[word] = freq.get(word, 0) + 1\n    return freq",
    },
    {
        "id": "015",
        "name": "find_second_largest",
        "category": "array",
        "difficulty": "medium",
        "bug_type": "missing_edge_case",
        "description": "Find the second largest unique value in an array",
        "bug_description": "Does not handle duplicate max values; fails when all elements are the same",
        "correct_code": "def find_second_largest(arr):\n    if len(arr) < 2:\n        return None\n    unique = list(set(arr))\n    if len(unique) < 2:\n        return None\n    unique.sort(reverse=True)\n    return unique[1]",
        "buggy_code": "def find_second_largest(arr):\n    if len(arr) < 2:\n        return None\n    arr_sorted = sorted(arr, reverse=True)\n    return arr_sorted[1]",
    },
    {
        "id": "016",
        "name": "validate_password",
        "category": "validation",
        "difficulty": "medium",
        "bug_type": "logic_inversion",
        "description": "Validate password: min 8 chars, at least one upper, one lower, one digit",
        "bug_description": "Uses 'or' instead of 'and' to combine checks, so any single criterion passes",
        "correct_code": "def validate_password(pw):\n    if len(pw) < 8:\n        return False\n    has_upper = any(c.isupper() for c in pw)\n    has_lower = any(c.islower() for c in pw)\n    has_digit = any(c.isdigit() for c in pw)\n    return has_upper and has_lower and has_digit",
        "buggy_code": "def validate_password(pw):\n    if len(pw) < 8:\n        return False\n    has_upper = any(c.isupper() for c in pw)\n    has_lower = any(c.islower() for c in pw)\n    has_digit = any(c.isdigit() for c in pw)\n    return has_upper or has_lower or has_digit",
    },
    {
        "id": "017",
        "name": "hex_to_rgb",
        "category": "conversion",
        "difficulty": "easy",
        "bug_type": "off_by_one",
        "description": "Convert hex color string like '#FF8800' to (r, g, b) tuple",
        "bug_description": "Slice indices are shifted by one, extracting wrong character groups",
        "correct_code": "def hex_to_rgb(hex_str):\n    hex_str = hex_str.lstrip('#')\n    r = int(hex_str[0:2], 16)\n    g = int(hex_str[2:4], 16)\n    b = int(hex_str[4:6], 16)\n    return (r, g, b)",
        "buggy_code": "def hex_to_rgb(hex_str):\n    hex_str = hex_str.lstrip('#')\n    r = int(hex_str[0:2], 16)\n    g = int(hex_str[2:4], 16)\n    b = int(hex_str[4:7], 16)\n    return (r, g, b)",
    },
    {
        "id": "018",
        "name": "compute_variance",
        "category": "statistics",
        "difficulty": "medium",
        "bug_type": "wrong_variable",
        "description": "Compute population variance of a list of numbers",
        "bug_description": "Divides by len(numbers)-1 (sample variance) instead of len(numbers)",
        "correct_code": "def compute_variance(numbers):\n    if not numbers:\n        return 0.0\n    mean = sum(numbers) / len(numbers)\n    return sum((x - mean) ** 2 for x in numbers) / len(numbers)",
        "buggy_code": "def compute_variance(numbers):\n    if not numbers:\n        return 0.0\n    mean = sum(numbers) / len(numbers)\n    return sum((x - mean) ** 2 for x in numbers) / (len(numbers) - 1)",
    },
    {
        "id": "019",
        "name": "wrap_text",
        "category": "text",
        "difficulty": "medium",
        "bug_type": "off_by_one",
        "description": "Wrap text to a given line width, breaking on spaces",
        "bug_description": "Uses > instead of >= for width check, allowing lines one character too long",
        "correct_code": "def wrap_text(text, width):\n    words = text.split()\n    lines = []\n    current_line = []\n    current_len = 0\n    for word in words:\n        if current_len + len(word) + len(current_line) > width:\n            lines.append(' '.join(current_line))\n            current_line = [word]\n            current_len = len(word)\n        else:\n            current_line.append(word)\n            current_len += len(word)\n    if current_line:\n        lines.append(' '.join(current_line))\n    return '\\n'.join(lines)",
        "buggy_code": "def wrap_text(text, width):\n    words = text.split()\n    lines = []\n    current_line = []\n    current_len = 0\n    for word in words:\n        if current_len + len(word) + len(current_line) >= width:\n            lines.append(' '.join(current_line))\n            current_line = [word]\n            current_len = len(word)\n        else:\n            current_line.append(word)\n            current_len += len(word)\n    if current_line:\n        lines.append(' '.join(current_line))\n    return '\\n'.join(lines)",
    },
    {
        "id": "020",
        "name": "heap_sort",
        "category": "algorithm",
        "difficulty": "hard",
        "bug_type": "logic_inversion",
        "description": "Sort a list using heap sort with in-place heapify",
        "bug_description": "Max-heapify comparison is inverted (< instead of >), building a min-heap and producing descending order",
        "correct_code": "def heap_sort(arr):\n    result = arr[:]\n    n = len(result)\n    def heapify(arr, n, i):\n        largest = i\n        left = 2 * i + 1\n        right = 2 * i + 2\n        if left < n and arr[left] > arr[largest]:\n            largest = left\n        if right < n and arr[right] > arr[largest]:\n            largest = right\n        if largest != i:\n            arr[i], arr[largest] = arr[largest], arr[i]\n            heapify(arr, n, largest)\n    for i in range(n // 2 - 1, -1, -1):\n        heapify(result, n, i)\n    for i in range(n - 1, 0, -1):\n        result[0], result[i] = result[i], result[0]\n        heapify(result, i, 0)\n    return result",
        "buggy_code": "def heap_sort(arr):\n    result = arr[:]\n    n = len(result)\n    def heapify(arr, n, i):\n        largest = i\n        left = 2 * i + 1\n        right = 2 * i + 2\n        if left < n and arr[left] < arr[largest]:\n            largest = left\n        if right < n and arr[right] < arr[largest]:\n            largest = right\n        if largest != i:\n            arr[i], arr[largest] = arr[largest], arr[i]\n            heapify(arr, n, largest)\n    for i in range(n // 2 - 1, -1, -1):\n        heapify(result, n, i)\n    for i in range(n - 1, 0, -1):\n        result[0], result[i] = result[i], result[0]\n        heapify(result, i, 0)\n    return result",
    },
    {
        "id": "021",
        "name": "count_consonants",
        "category": "string",
        "difficulty": "easy",
        "bug_type": "logic_inversion",
        "description": "Count consonants in a string (alphabetic non-vowels)",
        "bug_description": "Checks 'in' instead of 'not in' vowels, counting vowels instead of consonants",
        "correct_code": "def count_consonants(s):\n    vowels = 'aeiou'\n    count = 0\n    for c in s.lower():\n        if c.isalpha() and c not in vowels:\n            count += 1\n    return count",
        "buggy_code": "def count_consonants(s):\n    vowels = 'aeiou'\n    count = 0\n    for c in s.lower():\n        if c.isalpha() and c in vowels:\n            count += 1\n    return count",
    },
    {
        "id": "022",
        "name": "sieve_of_eratosthenes",
        "category": "math",
        "difficulty": "hard",
        "bug_type": "off_by_one",
        "description": "Return all primes up to n using the Sieve of Eratosthenes",
        "bug_description": "Sieve array size is n instead of n+1, missing the number n itself",
        "correct_code": "def sieve_of_eratosthenes(n):\n    if n < 2:\n        return []\n    size = n + 1\n    is_prime = [True] * size\n    is_prime[0] = False\n    is_prime[1] = False\n    limit = int(n ** 0.5) + 1\n    for i in range(2, limit):\n        if is_prime[i]:\n            start = i * i\n            for j in range(start, size, i):\n                is_prime[j] = False\n    primes = []\n    for i in range(2, size):\n        if is_prime[i]:\n            primes.append(i)\n    return primes",
        "buggy_code": "def sieve_of_eratosthenes(n):\n    if n < 2:\n        return []\n    size = n\n    is_prime = [True] * size\n    is_prime[0] = False\n    is_prime[1] = False\n    limit = int(n ** 0.5) + 1\n    for i in range(2, limit):\n        if is_prime[i]:\n            start = i * i\n            for j in range(start, size, i):\n                is_prime[j] = False\n    primes = []\n    for i in range(2, size):\n        if is_prime[i]:\n            primes.append(i)\n    return primes",
    },
    {
        "id": "023",
        "name": "remove_duplicates",
        "category": "list",
        "difficulty": "easy",
        "bug_type": "missing_edge_case",
        "description": "Remove duplicates from a list while preserving order",
        "bug_description": "Uses set() directly which does not preserve insertion order in older Python; but also returns a set instead of list",
        "correct_code": "def remove_duplicates(lst):\n    seen = set()\n    result = []\n    for item in lst:\n        if item not in seen:\n            seen.add(item)\n            result.append(item)\n    return result",
        "buggy_code": "def remove_duplicates(lst):\n    return list(set(lst))",
    },
    {
        "id": "024",
        "name": "invert_dict",
        "category": "dict",
        "difficulty": "easy",
        "bug_type": "wrong_variable",
        "description": "Invert a dictionary (swap keys and values)",
        "bug_description": "Maps key to key instead of value to key",
        "correct_code": "def invert_dict(d):\n    result = {}\n    for k, v in d.items():\n        result[v] = k\n    return result",
        "buggy_code": "def invert_dict(d):\n    result = {}\n    for k, v in d.items():\n        result[k] = k\n    return result",
    },
    {
        "id": "025",
        "name": "running_sum",
        "category": "array",
        "difficulty": "easy",
        "bug_type": "off_by_one",
        "description": "Compute running sum of an array",
        "bug_description": "Starts loop at index 2 instead of 1, skipping the second element",
        "correct_code": "def running_sum(arr):\n    result = arr[:]\n    for i in range(1, len(result)):\n        result[i] += result[i - 1]\n    return result",
        "buggy_code": "def running_sum(arr):\n    result = arr[:]\n    for i in range(2, len(result)):\n        result[i] += result[i - 1]\n    return result",
    },
    {
        "id": "026",
        "name": "is_valid_ipv4",
        "category": "validation",
        "difficulty": "medium",
        "bug_type": "off_by_one",
        "description": "Validate an IPv4 address string",
        "bug_description": "Checks < 255 instead of <= 255, rejecting valid address 255.255.255.255",
        "correct_code": "def is_valid_ipv4(ip):\n    parts = ip.split('.')\n    if len(parts) != 4:\n        return False\n    for part in parts:\n        if not part.isdigit():\n            return False\n        num = int(part)\n        if num < 0 or num > 255:\n            return False\n        if len(part) > 1 and part[0] == '0':\n            return False\n    return True",
        "buggy_code": "def is_valid_ipv4(ip):\n    parts = ip.split('.')\n    if len(parts) != 4:\n        return False\n    for part in parts:\n        if not part.isdigit():\n            return False\n        num = int(part)\n        if num < 0 or num >= 255:\n            return False\n        if len(part) > 1 and part[0] == '0':\n            return False\n    return True",
    },
    {
        "id": "027",
        "name": "pounds_to_kilograms",
        "category": "conversion",
        "difficulty": "easy",
        "bug_type": "logic_inversion",
        "description": "Convert weight from pounds to kilograms",
        "bug_description": "Multiplies instead of divides by 2.20462, converting kg to pounds",
        "correct_code": "def pounds_to_kilograms(lbs):\n    conversion_factor = 2.20462\n    if lbs < 0:\n        return 0.0\n    kg = lbs / conversion_factor\n    return round(kg, 4)",
        "buggy_code": "def pounds_to_kilograms(lbs):\n    conversion_factor = 2.20462\n    if lbs < 0:\n        return 0.0\n    kg = lbs * conversion_factor\n    return round(kg, 4)",
    },
    {
        "id": "028",
        "name": "compute_median",
        "category": "statistics",
        "difficulty": "medium",
        "bug_type": "off_by_one",
        "description": "Compute median of a list of numbers",
        "bug_description": "For even-length lists, uses n//2 and n//2+1 instead of n//2-1 and n//2",
        "correct_code": "def compute_median(numbers):\n    if not numbers:\n        return None\n    s = sorted(numbers)\n    n = len(s)\n    if n % 2 == 1:\n        return s[n // 2]\n    else:\n        return (s[n // 2 - 1] + s[n // 2]) / 2",
        "buggy_code": "def compute_median(numbers):\n    if not numbers:\n        return None\n    s = sorted(numbers)\n    n = len(s)\n    if n % 2 == 1:\n        return s[n // 2]\n    else:\n        return (s[n // 2] + s[n // 2 + 1]) / 2",
    },
    {
        "id": "029",
        "name": "truncate_text",
        "category": "text",
        "difficulty": "easy",
        "bug_type": "wrong_variable",
        "description": "Truncate text to max_len characters, adding '...' if truncated",
        "bug_description": "Slices to max_len instead of max_len-3, so result exceeds max_len",
        "correct_code": "def truncate_text(text, max_len):\n    if not text:\n        return text\n    if len(text) <= max_len:\n        return text\n    truncated = text[:max_len - 3]\n    return truncated + '...'",
        "buggy_code": "def truncate_text(text, max_len):\n    if not text:\n        return text\n    if len(text) <= max_len:\n        return text\n    truncated = text[:max_len]\n    return truncated + '...'",
    },
    {
        "id": "030",
        "name": "shell_sort",
        "category": "algorithm",
        "difficulty": "hard",
        "bug_type": "off_by_one",
        "description": "Sort a list using Shell sort with gap sequence n//2, n//4, ..., 1",
        "bug_description": "Gap halving uses gap //= 3 instead of gap //= 2, skipping gap=1 for some sizes and leaving array unsorted",
        "correct_code": "def shell_sort(arr):\n    result = arr[:]\n    n = len(result)\n    if n <= 1:\n        return result\n    gap = n // 2\n    while gap > 0:\n        for i in range(gap, n):\n            temp = result[i]\n            j = i\n            while j >= gap and result[j - gap] > temp:\n                result[j] = result[j - gap]\n                j -= gap\n            result[j] = temp\n        gap = gap // 2\n    return result",
        "buggy_code": "def shell_sort(arr):\n    result = arr[:]\n    n = len(result)\n    if n <= 1:\n        return result\n    gap = n // 2\n    while gap > 0:\n        for i in range(gap, n):\n            temp = result[i]\n            j = i\n            while j >= gap and result[j - gap] > temp:\n                result[j] = result[j - gap]\n                j -= gap\n            result[j] = temp\n        gap = gap // 3\n    return result",
    },
]
